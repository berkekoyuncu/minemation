<UserControl x:Class="Minemation.Desktop.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Margin="20">
        <!-- Harita Alanı -->
        <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="2" ClipToBounds="True">
            
            <Grid>
                <!-- Harita Arka Planı (Basit bir ızgara dokusu veya koyu renk zemin) -->
                <Canvas x:Name="MapCanvas" Background="{DynamicResource BackgroundBrush}" Opacity="0.9">
                    <!-- Süsleme amaçlı arka plan çizgileri eklenebilir -->
                </Canvas>
                
                <!-- Girişler Katmanı -->
                <Canvas x:Name="LayerEntrances" Visibility="{Binding IsEntrancesVisible, Converter={StaticResource BoolToVis}}">
                    <!-- Kuzey Girişi -->
                    <StackPanel Canvas.Left="150" Canvas.Top="100">
                        <materialDesign:PackIcon Kind="MapMarkerRadius" Width="48" Height="48" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                        <Border Background="{DynamicResource CardBrush}" CornerRadius="4" Padding="5,2" Margin="0,-10,0,0">
                            <TextBlock Text="Kuzey Girişi" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" />
                        </Border>
                    </StackPanel>
                    
                    <!-- Doğu Girişi -->
                    <StackPanel Canvas.Left="750" Canvas.Top="450">
                        <materialDesign:PackIcon Kind="MapMarkerRadius" Width="48" Height="48" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                        <Border Background="{DynamicResource CardBrush}" CornerRadius="4" Padding="5,2" Margin="0,-10,0,0">
                            <TextBlock Text="Doğu Tüneli" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12" />
                        </Border>
                    </StackPanel>
                </Canvas>

                <!-- Ekipman Katmanı -->
                <Canvas x:Name="LayerEquipment" Visibility="{Binding IsEquipmentVisible, Converter={StaticResource BoolToVis}}">
                    <StackPanel Canvas.Left="300" Canvas.Top="250">
                        <materialDesign:PackIcon Kind="Excavator" Width="36" Height="36" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="E-01" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" HorizontalAlignment="Center" />
                    </StackPanel>
                    
                    <StackPanel Canvas.Left="600" Canvas.Top="350">
                        <materialDesign:PackIcon Kind="Truck" Width="36" Height="36" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="K-02" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold" HorizontalAlignment="Center" />
                    </StackPanel>
                </Canvas>

                <!-- Personel Katmanı -->
                <Canvas x:Name="LayerPersonnel" Visibility="{Binding IsPersonnelVisible, Converter={StaticResource BoolToVis}}">
                    <StackPanel Canvas.Left="200" Canvas.Top="200">
                        <materialDesign:PackIcon Kind="AccountHardHat" Width="28" Height="28" Foreground="{DynamicResource SuccessBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Ahmet Y." Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                    
                    <StackPanel Canvas.Left="450" Canvas.Top="220">
                        <materialDesign:PackIcon Kind="AccountHardHat" Width="28" Height="28" Foreground="{DynamicResource SuccessBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Mehmet K." Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                    
                    <StackPanel Canvas.Left="650" Canvas.Top="400">
                        <materialDesign:PackIcon Kind="AccountHardHat" Width="28" Height="28" Foreground="{DynamicResource WarningBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Ali V." Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" HorizontalAlignment="Center" />
                    </StackPanel>
                </Canvas>

                <!-- Sağ Üst: Katman Filtreleri Paneli (Glassmorphism / Overlay) -->
                <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Background="{DynamicResource CardBrush}" CornerRadius="8" Padding="20" 
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Opacity="0.95">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="15" Opacity="0.4" ShadowDepth="4" />
                    </Border.Effect>
                    
                    <StackPanel Width="220">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <materialDesign:PackIcon Kind="LayersOutline" Width="24" Height="24" Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="Harita Katmanları" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,15" Height="1" />

                        <CheckBox Content="Personeller" IsChecked="{Binding IsPersonnelVisible}" Style="{StaticResource MaterialDesignCheckBox}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" Margin="0,5" />
                        <CheckBox Content="Ekipmanlar" IsChecked="{Binding IsEquipmentVisible}" Style="{StaticResource MaterialDesignCheckBox}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" Margin="0,5" />
                        <CheckBox Content="Maden Ocağı Girişleri" IsChecked="{Binding IsEntrancesVisible}" Style="{StaticResource MaterialDesignCheckBox}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" Margin="0,5" />
                    </StackPanel>
                </Border>

                <!-- Sol Alt: Yakınlaştırma Kontrolleri -->
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="20">
                    <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10" ToolTip="Yakınlaştır">
                        <materialDesign:PackIcon Kind="Plus" Width="24" Height="24" />
                    </Button>
                    <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource BorderBrush}" Foreground="{DynamicResource TextPrimaryBrush}" ToolTip="Uzaklaştır">
                        <materialDesign:PackIcon Kind="Minus" Width="24" Height="24" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
